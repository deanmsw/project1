<h1>All users</h1>

<h2><%= User.count %> Users</h2>

<% @users.each do |user| %>
  <p><%= user.email %></p>

  <%= button_to 'Delete User', user, :method => 'delete', :data => {:confirm => 'Are you sure?'}, :class => 'spacing' %>

<% end %>
