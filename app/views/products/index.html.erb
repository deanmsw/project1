<h1>All Products</h1>

<% @products.each do |product| %>

<%= image_tag product.image, :class => 'thumb' if product.image.present? %>
  <ul class='index'>
    <li> <%= link_to product.name, product %> </li>
    <li> Price = $ <%= product.price %> </li>
    <li> Location: <%= product.location%> </li>
  </ul>
<% end %>
